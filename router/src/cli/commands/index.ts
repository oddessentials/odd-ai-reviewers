/**
 * CLI Commands Module Exports
 *
 * Barrel export for CLI command handlers.
 */

// Local review command will be exported when implemented (Phase 8)
// export { runLocalReview, type LocalReviewDependencies } from './local-review.js';

/**
 * Command registry version - used for module loading verification
 * This will be replaced by actual command exports in Phase 8
 */
export const COMMANDS_MODULE_VERSION = '1.0.0' as const;
