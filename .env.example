# API keys — at least one required for benchmark recording (RECORD=true)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-proj-...

# Benchmark model pinning — required for snapshot recording
BENCHMARK_MODEL_ID=gpt-4o
BENCHMARK_PROVIDER=openai
