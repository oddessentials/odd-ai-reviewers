{
  "name": "odd-ai-reviewers",
  "version": "1.0.0",
  "description": "Extensible AI code review system with pluggable agents",
  "private": true,
  "workspaces": [
    "router"
  ],
  "scripts": {
    "build": "npm run build --workspace=router",
    "test": "npm run test --workspace=router",
    "lint": "npm run lint --workspace=router",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "code-review",
    "ai",
    "github-actions",
    "semgrep",
    "opencode"
  ],
  "author": "OddEssentials",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
